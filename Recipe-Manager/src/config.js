// src/config.js
// This file holds your backend API base URL.
// You can change it later when deploying to another server.

const config = {
  // Make sure this matches your backend Tomcat base path
  url: "http://localhost:2030/RecipeReact/api"
};

export default config;
